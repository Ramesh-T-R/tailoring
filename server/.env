PORT=5001
# Local MongoDB (Default)
MONGODB_URI=mongodb://localhost:27017/thaiyalagam

# MongoDB Atlas (Uncomment and update placeholders to use cloud DB)
# MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/thaiyalagam?retryWrites=true&w=majority
